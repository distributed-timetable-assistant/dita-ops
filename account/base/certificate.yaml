apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: account
spec:
  secretName: account-tls
  dnsNames:
    - "dita-account.hasankarimi.ir"
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
