apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: basic-scheduler
spec:
  secretName: basic-scheduler-tls
  dnsNames:
    - "bs.st.dita.hasankarimi.ir"
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
