apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: basic-scheduler
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - bs.st.dita.hasankarimi.ir
      secretName: basic-scheduler-tls
  rules:
    - host: bs.st.dita.hasankarimi.ir
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: basic-scheduler
                port:
                  name: http
