apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: demo-yew
spec:
  secretName: demo-yew-tls
  dnsNames:
    - "demo-yew.outi.ir"
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
