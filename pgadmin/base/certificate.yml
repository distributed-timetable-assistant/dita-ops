apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pgadmin
spec:
  secretName: pgadmin-tls
  dnsNames:
    - "pgadmin.st.dita.hasankarimi.ir"
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
